services:
  falkordb:
    image: falkordb/falkordb:latest
    ports:
      - "6379:6379"
    volumes:
      - falkordb_data:/data
    environment:
      - FALKORDB_ARGS=TIMEOUT 0 save 300 1 stop-writes-on-bgsave-error no
    restart: unless-stopped

volumes:
  falkordb_data:
